{
    "name": "alexsanro_portfolio",
    "postCreateCommand": "npm install",
    "dockerComposeFile": ["docker-compose.yml"],
    "service": "alexsanro_portfolio",
    "workspaceFolder": "/app",
    "shutdownAction": "stopCompose",
    "settings": {
        "files.associations": {
            "[Dd]ockerfile*": "dockerfile"
        }
    }
}
